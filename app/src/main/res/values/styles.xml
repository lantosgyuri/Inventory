<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Warning_Toast">
        <item name="textBold">true</item>
        <item name="textColor">#FFFFFF</item>
        <item name="textSize">14sp</item>
        <item name="colorBackground">#F44336</item>
        <item name="solidBackground">true</item>
        <item name="iconLeft">@drawable/ic_clear_black</item>
        <item name="iconRight">@drawable/ic_clear_black</item>
        <item name="length">LONG</item>
    </style>

    <style name="Success_Toast">
        <item name="textBold">true</item>
        <item name="textColor">#FFFFFF</item>
        <item name="textSize">14sp</item>
        <item name="colorBackground">#4CAF50</item>
        <item name="solidBackground">true</item>
        <item name="iconRight">@drawable/ic_check_black</item>
        <item name="length">LONG</item>
    </style>




</resources>
